# Copy this template to .env and set real credentials before running.

DATABASE_URL=postgresql+psycopg2://user:password@host:port/dbname?sslmode=require

# SMTP configuration
SMTP_HOST=mail.example.com
SMTP_PORT=465
SMTP_USER=info@example.com
SMTP_PASS=change-me
MAIL_RECEIVER=info@example.com
